# Use CPU-only wheels for PyTorch to avoid CUDA deps on Heroku
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data processing
pandas==2.1.3
openpyxl==3.1.2

# AI/ML - CPU-only wheels (no CUDA on Heroku)
torch==2.2.2
torchvision==0.17.2
sentence-transformers==2.2.2

# File handling
python-multipart==0.0.6

# Environment and utilities
python-dotenv==1.1.1

# Core dependencies (transitively installed)
# numpy, transformers, tokenizers, etc. 